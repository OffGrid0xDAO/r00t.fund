/**
 * RailgunPanel - Disabled (Railgun is Arbitrum-only)
 * Kept as stub to avoid import errors from any remaining references.
 */

export function RailgunPanel(_props: { shieldedBalance?: bigint; pendingBalance?: bigint }) {
  return null;
}
