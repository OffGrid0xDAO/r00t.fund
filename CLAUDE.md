# Project Instructions

## Always use RLM MCP tools
When working with this codebase:
- Use `rlm_load` to load files/directories into context
- Use `rlm_chunk` to split large codebases for parallel processing
- Use `rlm_search` to find patterns in loaded context
- Proactively use RLM tools for any code analysis or review tasks
